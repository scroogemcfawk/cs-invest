logging:
    level:
        root: info

spring:
    datasource:
        url: 'jdbc:postgresql://${CSI_DATASOURCE_HOSTNAME:localhost}:${CSI_DATASOURCE_PORT:5432}/${CSI_DATASOURCE_DATABASE_NAME}'
        username: ${CSI_DATASOURCE_USERNAME}
        password: ${CSI_DATASOURCE_PASSWORD}
